import React from 'react'

export default function navbar() {
    return (
      <nav className="navbar">
        <button className="navbutton">array size</button>
        <button className="navbutton">generate array</button>
        <button className="navbutton">algorithm</button>
        <button className="navbutton">run</button>
      </nav>
    );
}
